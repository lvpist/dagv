[project]
name = "dagv"
authors = [
    { name = "DAGV", email = "ti@dagv.local" }
]
description = "DAGV — member registration, areas, ranks & provisioning for the Diretório Acadêmico Getúlio Vargas (FGV)"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "dagv"
